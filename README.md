# km-ps2-abi

Userspace ABI constants for km-ps2 (PS/2 controller) -- extracted from
km-ps2 into its own repository so consumers (um-keyboard-ps2) depend
on the stable contract, never on driver implementation details.
